# CLI

Empty until **Sprint 19**.

`shellwright` — config-as-code, source export, and builds triggered from a
customer's own pipeline.

This is a differentiator rather than a convenience. The category is built on
lock-in; a first-class CLI and a real source export say that leaving is
supported, which is a much easier thing to sell against.
